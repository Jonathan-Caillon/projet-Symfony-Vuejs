<form method="post" action="{{ path('gros_delete', {'id': gro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gro.id) }}">
    <button class="btn">Delete</button>
</form>
